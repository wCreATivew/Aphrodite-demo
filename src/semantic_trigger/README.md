# Semantic Trigger Ownership Marker

> **Architecture ownership marker - Legacy Supported Path**
>
> `src/semantic_trigger/` is not part of the current embodied runtime mainline. It remains supported because CLI entrypoints and tests depend on its interfaces and behavior. Treat changes here as compatibility/support work unless the path is deliberately integrated into the current runtime architecture.

